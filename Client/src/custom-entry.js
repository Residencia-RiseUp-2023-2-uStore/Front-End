import { createApp } from 'vue'; // Use a importação adequada para o seu projeto
import Sidebar from './Componentes/global/Sidebar'; // Importe o componente apropriado

const app = createApp(Sidebar);
app.mount('#app');